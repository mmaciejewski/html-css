/* jshint jquery: true, browser: true */

var depth = function() {
    
    
};

$(function() {
  var depth = function(root) {
      var $children = $( root ).children();
      var depth = 0;
      
      while($children )
      
  };
   alert(depth()); 
});